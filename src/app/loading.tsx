export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center px-4 py-12" aria-busy="true">
      <p className="text-sm text-muted-foreground" role="status">Cargando MediAbsence…</p>
    </main>
  );
}
